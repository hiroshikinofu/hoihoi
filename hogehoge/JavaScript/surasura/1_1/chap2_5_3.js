// if文と比較する式を組み合わせる
let text = prompt('年齢は？');
let age = parseInt(text);
if(age < 20){
    console.log('未成年');
}

// 文字列'年齢は？'を表示し、ユーザに値を入力させ、変数'text'に入れろ
// 変数'text'を整数化し、変数'age'に入れろ
// もし、変数'age'の値が20以下の場合、
// 文字列'未成年'を表示させろ