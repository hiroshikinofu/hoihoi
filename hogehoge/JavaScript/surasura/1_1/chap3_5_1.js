// 九九の計算をしてみよう
for(let x = 1; x < 10; x++){
  for(let y = 1; y < 10; y++){
    console.log(x * y);
  }
}

// 変数'x'に初期値'1'を設定。変数'x'の値が10以上になるまで、以下の処理を繰り返せ
// 変数'y'に初期値'1'を設定。変数'y'の値が10以上になるまで、以下の処理を繰り返せ
// 変数'x'* 変数'y'の計算式の値を表示しろ
// 変数'y'の値を1増やす
// 変数'x'の値を1増やす