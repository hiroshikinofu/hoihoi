// 九九らしく表示する
for(let x = 1; x < 10; x++ ){
    for(let y = 1; y < 10; y++){
        console.log(x + '✖' + y + '=' + x * y);
    }
}

// 変数'x'の初期値1を設定する。変数'x'が10以上になるまで、以下の処理を繰り返せ。
// 変数'y'の初期値1を設定する。変数'y'が10以上になるまで、以下の処理を繰り返せ。
// 変数'x'と、文字列'バツ'と、変数'y'と、文字列'＝'と変数'x'*変数'y'の値を結合して表示しろ。
// 変数'y'の値を1増やせ
// 変数'x'の値を1増やせ