// 元の値に7%を上乗せする関数を定義する
let addCharge = (bill) => {
    return bill * 1.07;
};
console.log(addCharge(40000));

// 引数'bill'を受けとる以下の関数を作り、変数'addCharge'に入れろ。
// {引数'bill'に数値'1.07'を掛けた値を呼び出し元に戻せ。}
// 4000をに手数料関数に入れて、戻り値を表示しろ。
